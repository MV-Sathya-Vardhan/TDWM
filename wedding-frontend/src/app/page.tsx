export default function Home() {
  return (
    /**
     * Homepage contents
     * 1. Hero component(carousel, video)
     * 2. Slider
     * 3. Zoom -in
     * 4. Stories - (Redirects to Single Wedding story)
     */
    <div>Hello Vaibhav</div>

  );
}
